<footer class="site-footer">
  <div>
    <div class="left-content">
      <img src="../public/imgs/logo2.svg" alt="Bagerdygtigt Logo" class="footer-logo" />
      <div class="footer-column left">
        <p class="footer-titel-2">Danmarks økologiske bager</p>
        <a href="#">Butikker</a>
        <a href="#">Se smiley rapport</a>
      </div>
    </div>
  </div>

  <div class="footer-column center">
    <!-- <p class="footer-titel">Følg os på:</p> -->
    <a href="#" target="_blank">Instagram</a>
  </div>

  <div class="footer-column right">
    <p class="footer-titel">+45 44 40 22 24 <br />Kundeservice@bagerdygtigt.dk</p>
    <p class="footer-titel">Est. 2012 © Bagerdygtigt</p>
  </div>
</footer>

<style>
  .site-footer {
    padding: 2rem 1.5rem;
    display: grid;
    gap: 2rem;
    text-align: center;
    grid-template-columns: 1fr;
  }

  /* Fjern margin på p for bedre vertikal kontrol */
  .site-footer p {
    margin: 0;
    padding: 0.25rem 0; /* evt. luft uden margin */
  }

  .footer-logo {
    margin: 0 auto 1.5rem auto;
    width: 90px;
    height: auto;
    display: block;
  }

  .footer-column a {
    display: block;
    margin: 0.25rem 0;
    transition: color 0.3s ease;
  }

  /* Fjern align-self: end for footer-column.right - det skaber problemer */
  .footer-column.right {
    /* align-self: end; fjernet */
  }

  /* Mobil: vertikal centering af footer-columns left og right */
  @media (max-width: 767px) {
    .site-footer {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .left-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .footer-column.left,
    .footer-column.right {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }
  }

  /* Desktop styles */
  @media (min-width: 768px) {
    .site-footer {
      grid-template-columns: 1fr 1fr 1fr;
      padding: 2rem 3rem;
      align-items: stretch; /* sikrer at alle grid-celler får samme højde */
    }

    .left-content {
      height: 100%;
      text-align: left;
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 1.5rem;
      align-items: center;
    }

    .footer-logo {
      margin: 0 auto;
    }

    .footer-column.left,
    .footer-column.right,
    .footer-column.center {
      display: flex;
      flex-direction: column;
      height: 100%;
      justify-content: space-between;
    }

    /* Kun denne regel ændres til: */
    .footer-column.center {
      display: flex;
      flex-direction: column;
      height: 100%;
      justify-content: flex-end; /* vigtig ændring */
      align-items: center; /* vigtig ændring */
      text-align: center;
      gap: 0.5rem;
    }

    .footer-column.right {
      text-align: right;
    }
  }
</style>
